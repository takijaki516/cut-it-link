import { Button } from "../ui/button";

function renderSigninDialog() {
  <SigninDialog>
    <Button size="sm">Sign In</Button>
  </SigninDialog>
}

export async function UserProfile() {

  const session = await getSer

}